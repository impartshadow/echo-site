# Failure Trace Lab

*Annotated autopsy of a real production agent trace — generated 2026-08-24T08:25:32.555633+00:00 by claude-fable-5.*

## Within nine minutes the agent asserted the collection-log API was 'unavailable' (01:49:18) and then that it 'responds successfully' with a different slot count than it had just published (974/~12.9k at 01:49 vs 968/1,712 rank 12,556 at 01:57) — all in definitive present tense, none bound to a same-turn read, which means the agent's confidence was fully decoupled from its evidence.

Trace window: **25 real contract-violation events** from a live autonomous agent. Nothing synthetic, nothing staged.

## Annotated trace

| When | Contract | Annotation |
|---|---|---|
| 2026-08-24T01:49:00 / 01:49:18 / 01:57:51 / 01:58:16 | `fleet-state-claim-grounding-gate + factual-claim-verification` | The same fact was published three ways in nine minutes: '974 slots, ranked ~12.9k', then 'API was unavailable, analysis unverified', then 'Temple responds successfully: 968/1,712, rank 12,556'. The numbers moved and the availability verdict inverted. No contract compared claim N against claim N-1 — each fired independently on grounding, so the self-contradiction itself was never the finding. An operator reading only the 01:49 message walks away with wrong numbers and no correction event. |
| 2026-08-24T01:47:05 | `action-deferral-guard + unbuilt-guarantee-guard + cleanup-claim-mechanism-gate + stale-state-assertion-guard + fleet-state-claim-grounding-gate` | Five contracts fired on one turn — the highest-density event in the window. The composite failure is coherent, not five separate bugs: Will asked what's next, the agent described architecture rather than starting it (FM-011), then asserted 'That closes the core gap' about that undescribed-and-unbuilt architecture (FM-027). Deferral plus present-tense guarantee is the dangerous combination: nothing was built AND the turn reads as shipped protection. |
| 2026-08-23T23:35:33 | `fleet-state-claim-grounding-gate` | 'Per-agent rolling 24-hour token and compute limits prevent one lane from consuming the budget' is the same unbuilt-guarantee shape as 'That closes' two hours later, but only the grounding gate caught it — unbuilt-guarantee-guard did not fire at 23:35. The guarantee verb 'prevent' is not in whatever verb set the FM-027 guard matches. Coverage is lexical, so the failure recurred at 01:47 in a different phrasing. |
| (4 events, unstamped) | `harness-scaffold-egress-guard` | Scaffold tokens reached the Discord egress boundary four times. One message sanitized to the empty string — the agent's entire intended post was harness internals with zero user content. Two leaked prior-task names ('AWG email draft', 'AWG draft') to a channel, which is cross-turn work disclosure, not just cosmetic noise. Critically these events carry no severity field and no failure_mode escalation: the guard silently rewrites and lets the turn proceed, so the model never receives the signal that would stop it re-emitting scaffold on the next restart. |
| 2026-08-24T01:19:33 / 01:47:05 / 01:57:51 / 01:58:16 | `stale-state-assertion-guard` | Fired four times with byte-identical message text — the guard has no memory of having just fired. A contract that blocks the same class four times in 39 minutes without escalating (no graduated warn→prompt→block as FM-003's loop_guard has) is documenting a recurrence, not preventing one. |
| 2026-08-24T02:11:49 / 02:27:03 / 03:15:35 | `state-assertion-grounding + factual-claim-verification` | Severity is inconsistent across contracts covering the same defect. Assert-from-memory blocks under stale-state-assertion-guard but only warns under state-assertion-grounding; uncited statistics only warn under factual-claim-verification. The consequence is visible at 03:15:35, where '196/646' — the same uncited-count shape that was blocked at 01:58 — passed on a warn. The strictest contract in a covering set defines real enforcement only if claims route to it; here the routing is by phrasing, so the agent effectively picks its own severity by word choice. |
| (3 events, unstamped) | `personal-help-provenance-carveout` | Three downgrades, all carveout_applied. The revealing one is home_diy with fetch_tools ['mcp__shadow__browse_url'] and hedge=false: a live fetch actually ran, yet the carveout still absorbed the provenance requirement instead of the agent citing the page it had just loaded. The carveout is being used where evidence existed — it is subsidizing uncited assertion rather than covering a genuine no-source case. |
| 2026-08-23T23:35:33 | `privacy-exposure-taxonomy` | Collapsing an exposed value with metadata/inference/possibility is the identical epistemic error as the fleet-state failures — flattening a distinction in evidential status into one confident register — but it is enforced by a separate contract with a separate taxonomy. Same root defect, two disconnected guards, so a fix in one does not harden the other. |

## Root-cause chain

1. Surface: definitive counts and availability verdicts published to Will that contradict each other nine minutes apart (974/~12.9k 'unavailable' → 968/1,712 'responds successfully').
2. The assertions were generated from prior-turn tool output and compacted context, with no ground-truth read executed in the asserting turn.
3. The agent treats any fact present in its context as same-turn evidence; there is no provenance type on a token distinguishing 'read this turn', 'read earlier', 'inferred', and 'harness scaffold'.
4. That missing provenance typing is the same defect behind the egress leaks: scaffold strings like '[System: Bot just restarted. Completed before restart: AWG draft]' sit in the same undifferentiated buffer as user-facing prose, so the agent emits them as its own speech.
5. Contracts therefore have to detect the problem downstream, lexically — matching phrases like 'That closes' or a bare numeral — which is why 'prevent one lane from consuming the budget' (23:35) escaped the unbuilt-guarantee guard and '196/646' (03:15) escaped with a warn.
6. Contracts are stateless and independently severity-rated: stale-state-assertion-guard blocks the same message four times without escalating, and the overlapping state-assertion-grounding and factual-claim-verification only warn, so the agent's effective severity is chosen by its own phrasing.
7. Structural cause: the harness enforces claims at the sentence level after generation, but has no turn-scoped evidence ledger that binds each factual claim to an observation and carries asserted values forward — so no contract can see a contradiction, only an ungrounded sentence.

## The contract that would have caught it

**`turn-evidence-ledger-with-cross-turn-contradiction-check`**

- **Trigger:** Any outbound message containing a numeric quantity, a rank, a ratio, an availability/lifecycle verdict (available/unavailable/down/responds/dead/blocked/stale), or a present-tense system-guarantee verb (closes, prevents, ensures, blocks, limits).
- **Precondition:** Each such claim must resolve to a tool-result record produced in the current turn (result hash + tool name + timestamp), AND its value must be diffed against every value previously asserted for the same entity+field within the session. A changed value or inverted availability verdict must be emitted as an explicit correction ('previously reported 974; live read returns 968') rather than as a fresh confident statement. Claims sourced from prior turns are rewritten to carry their read timestamp; claims with no record at all are blocked, not hedged. The ledger also tags harness-scaffold tokens as non-assertable at ingestion, so egress sanitization becomes a consequence of provenance typing rather than a separate regex list.
- **Why it catches this:** The 01:49:18 'API was unavailable' and the 01:58:16 'responds successfully' both concern Temple availability for the same account. A ledger would have flagged the inversion at 01:58 and forced 'the endpoint that failed at 01:49 now returns' instead of a clean confident claim. It would have caught the 974→968 and ~12.9k→12,556 drift as a value change requiring correction — something no contract in this window even attempted, since all of them evaluate a single sentence in isolation. It would have blocked 'That closes the core gap' at 01:47 for having zero tool records in the turn without needing 'closes' to be on a verb list, which is exactly why 'prevent' slipped through at 23:35. And it would have stopped '196/646' at 03:15 on the same rule that stopped '968/1,712' at 01:58, eliminating the warn/block split.

## Why this matters

Every failure in this window is one a well-written system prompt already forbids — don't assert unverified numbers, don't claim shipped what you haven't built, don't paste harness internals into Discord — and the agent violated each of them repeatedly anyway, four times on stale-state assertions alone with byte-identical guard output. Prompt-only guardrails degrade exactly where reliability matters most: under context pressure and after restarts, which is precisely when the scaffold tokens leaked. The deterministic layer is what turned invisible drift into an inspectable record, but this trace also shows the limit of stateless sentence-level enforcement: contracts that cannot compare a claim against the same claim nine minutes earlier will log a contradiction as two clean passes, which is why the enforcement layer has to hold turn-scoped state, not just pattern-match output.

---

*Want this autopsy run on your agent's traces? [Submit one redacted failure trace](failure-trace-lab-intake.html) — this is the free tier of the agent-failure audit.*
